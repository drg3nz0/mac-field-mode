#!/bin/zsh
# Double-click launcher — arms field-mode (battery-save + wifi-shield)
clear
echo "🌍 FIELD MODE ON — Mac password requested once..."
"$HOME/.local/bin/field-mode" on
echo ""
echo "Window can be closed. (Cmd+W)"
